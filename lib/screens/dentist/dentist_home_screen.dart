import 'package:flutter/material.dart';

class DentistHomeScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text("Dentist Home")),
      body: Center(child: Text("Welcome, Dentist!")),
    );
  }
}
